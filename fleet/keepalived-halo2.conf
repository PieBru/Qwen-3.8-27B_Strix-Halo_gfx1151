# fleet/keepalived-halo2.conf — strixy-9ad3 (.15, VRRP BACKUP, priority 100)
# Deployed to /etc/keepalived/keepalived.conf
vrrp_instance LLAMAS {
    state BACKUP
    interface eno1
    virtual_router_id 51
    priority 100
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass Halo2Hal0
    }
    virtual_ipaddress {
        192.168.50.10/24
    }
}
