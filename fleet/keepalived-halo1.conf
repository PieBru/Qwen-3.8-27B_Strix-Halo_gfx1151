# fleet/keepalived-halo1.conf — strixy2 (dev halo, VRRP MASTER, priority 150)
# Deployed to /etc/keepalived/keepalived.conf
vrrp_instance LLAMAS {
    state MASTER
    interface eno1
    virtual_router_id 51
    priority 150
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass Halo2Hal0
    }
    virtual_ipaddress {
        192.168.50.10/24
    }
}
