# Model weights — far too large for git (back these up separately)
*.gguf

# Upstream fork clone + its build artifacts (has its own git history)
llama.cpp/

# Runtime logs / temp
*.log
nohup.out
results/
llama.cpp-stock/
lc-edge/

# Operator's local helper: resume the previous pi session (1-liner).
# Local-only by design — never commit or delete.
pi-resume.sh
__pycache__/
docs/archive/
